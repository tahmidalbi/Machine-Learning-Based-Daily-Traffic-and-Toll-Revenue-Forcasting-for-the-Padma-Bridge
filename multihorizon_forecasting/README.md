# Padma Bridge Multi-Horizon Forecasting

Independent implementation of `ashique portion work 2.pdf`. It trains global
direct 1–30 day traffic and toll models using origin-date-safe features.

Run `python train_multihorizon.py`. Outputs are under `outputs/`, estimators under
`models/`, and the final report is `multihorizon_model_training_results.pdf`.

